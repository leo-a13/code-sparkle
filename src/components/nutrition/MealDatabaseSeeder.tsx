import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const MealDatabaseSeeder = () => {
  return (
    <Card>
      <CardHeader><CardTitle>Meal Database Management</CardTitle></CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Database features removed.</p>
      </CardContent>
    </Card>
  );
};

export default MealDatabaseSeeder;
